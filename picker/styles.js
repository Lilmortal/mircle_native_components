import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  picker: {
    borderColor: "black",
    borderBottomWidth: 1,
    marginTop: 10,
    marginBottom: 10
  }
});

export default styles;
